# FastAPI backend dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Supabase client
supabase>=2.0.0

# Document parsing
PyPDF2>=3.0.0
python-docx>=1.1.0
llama-cloud-services>=0.6.0  # LlamaParse for accurate PDF section extraction
PyMuPDF>=1.24.0  # Required by LlamaExtract for structured extraction

# Table extraction handled by LlamaParse with parsing instructions

# SAP Generator dependencies
groq>=0.4.0
anthropic>=0.40.0
openai>=1.12.0
networkx>=3.2.0
numpy>=1.24.0
scipy>=1.11.0

# Note: rpy2 installed separately in build command after R is available

# Agentic HybridRAG Pipeline dependencies
chromadb>=0.4.0

# CPU-only torch (~200MB instead of 2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2.0
